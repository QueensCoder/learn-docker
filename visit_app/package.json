{
  "name": "visit_app",
  "version": "1.0.0",
  "description": "app that uses redis and express to track how many times users visits the node server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  },
  "author": "Queens Coder",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^1.19.4",
    "redis": "^2.8.0"
  }
}
